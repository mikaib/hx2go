package language;

function run() {
    trace("Immediate Constants\n");
    assert(true, true);
    assert(false, false);
    assert("hello", "hello");
    assert("", "");
    assert(3.14, 3.14);
    assert(0.0, 0.0);
}
